import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  timer: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
    marginBottom: 20,
  },
});

export default styles;